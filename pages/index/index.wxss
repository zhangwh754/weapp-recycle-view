page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #f5f5f5;
}

.scroll-area {
  flex: 1;
  overflow-y: hidden;
  height: 100%;
}

.product-list {
  /* 移除padding，确保高度计算准确 */
}